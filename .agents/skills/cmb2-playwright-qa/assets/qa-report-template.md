# CMB2 browser QA report

Local-only report. Do not publish or post this report without approval.

## Run

- Date:
- Target:
- Branch and revision:
- Input mode: procedure | conversation
- Environment configuration: describe keys, never values for credentials

## Claims

One row per claim. Result is `pass`, `fail`, or `skipped` — never blank, and `skipped` always states why.

| Claim | Steps | Result | Evidence |
| --- | --- | --- | --- |
| 1 |  | pass | Add verified relative artifact links only. |

## Failures and limitations

- Record final URL, failed assertion or locator, and final-state capture path.
- Record skipped claims and why.

## Follow-up

- State whether the run meets the graduation criteria for `tests/playwright/`.
