# Shell and CI environment variables override these local development values.
WP_BASE_URL=http://localhost:2623
WP_USERNAME=admin
WP_PASSWORD=replace-with-local-password

# Use 1 only when WordPress is already running or its availability is known.
SKIP_WP_SERVER=0
SKIP_WP_CHECK=0
